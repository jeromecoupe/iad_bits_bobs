# Test blog for 11ty workshop

Small test blog for my 11ty workshop

## Installation

- install node and npm (latest)
- `npm i` to install dependencies
- `npm run build` to build the project
- `npm run dev` to run a development server
